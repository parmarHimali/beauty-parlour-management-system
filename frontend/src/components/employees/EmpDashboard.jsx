import EmployeeCalendar from "./EmployeeCalendar";
const EmpDashboard = () => {
  return (
    <div className="appointments-list" style={{ width: "80%" }}>
      <EmployeeCalendar />
    </div>
  );
};

export default EmpDashboard;
